// Condicionales
const disponible = 4000
const retirar = 2000

if (disponible > retirar) {
    // Se cumple la condicion y se ejeuta el codigo
    console.log('Retiro exitoso');
} else {
    console.log('No hay suficiente dinero');
}

/**
 *  > - Mayor que
 *  < - Menor que
 * >= - Mayor o igual que
 * <= - Menor o igual que
 */
